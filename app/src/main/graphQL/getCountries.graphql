query getCountries($code:ID!){
    continent(code:$code){
        countries{
            name
            native
            phone
            capital
            currency
            emojiU
        }
    }
}